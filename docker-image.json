{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json", 
  "extends": [
    "local>DanySK/renovate-config"
  ],
  "packageRules": [
    {
      "description": "Trigger a patch update if any Dockerfile gets updated",
      "matchFileNames": [
        "**/Dockerfile"
      ],
      "semanticCommitScope": "core-deps"
    }
  ]
}
